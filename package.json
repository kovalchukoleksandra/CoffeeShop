{
  "name": "coffeeshop-backend",
  "version": "1.0.0",
  "description": "Бекенд для CoffeeShop",
  "main": "index.js",
"scripts": {
  "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
},
  "dependencies": {
    "json-server": "^0.17.4"
  }
}